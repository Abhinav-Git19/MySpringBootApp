### Profile SpringBoot Application
This repository implements basic Profile Application with all CRUD operations
I have also a setup persistence layer where any data posted will persists through MySQL DB
